{
  "name": "gestaoeco",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test js/fechamento.test.js",
    "test:rules": "firebase emulators:exec --only firestore \"node --test firestore.rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "fast-check": "^3.23.1",
    "firebase": "^12.15.0",
    "firebase-tools": "^15.22.4"
  }
}
